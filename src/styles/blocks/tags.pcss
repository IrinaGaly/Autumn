.tags {
  display: flex;
  flex-direction: row;
  margin-bottom: 35px;
}
.tags__item {
  margin-right: 8px;
  padding: 15px 20px;
  text-decoration: none;
  border-radius: 30px;
  background:  #F3F6F9;
  display: inline-block;
}

.tag {
  font-weight: 600;
  font-size: 15px;
  color: rgba(51, 60, 73, 0.8);
  
}